import React from 'react'
import { useAuth } from '../hooks/useAuth'
import {addComment, editComment, deleteComment} from '../api/commment.js'
import {likeComment, unlikeComment} from '../api/like.js'
const CommentCard = ({comment}) => {
  return (
    <>
      
    </>
  )
}

export default CommentCard
